version: '3.8'

services:
  newshub:
    build: .
    container_name: newshub
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
